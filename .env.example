# Backend API URL
VITE_API_URL=http://localhost:3000/api/v1

# WebSocket URL
VITE_WS_URL=http://localhost:3000

# Meta (Facebook) Pixel ID — only used by the Tech AI Future Uyo event page
# (src/pages/events/TechAiFutureUyo.tsx). Leave unset to disable tracking
# (e.g. in local dev); get the real value from Events Manager.
VITE_META_PIXEL_ID=
